const movimentosId = {
    movimentos : require('../models/movimentos.json'),
    setMovimrntos : function(dados) {this.movimentos = data}
}

const fsPromises = require('fs').promises;
const path = require('path');

